server:
  servlet:
    context-path: /

spring:
  liquibase:
    enabled: false
  jpa:
    show-sql: true

logging:
  level:
    org:
      springframework: trace
      hibernate:
        validator: trace